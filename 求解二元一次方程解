a=float(input('请输入a：'))
b=float(input('请输入b：'))
c=float(input('请输入c：'))
import math
def quadratic(a, b, c):
    if b**2-4*a*c>=0:
        x1=(-b-math.sqrt(b**2-4*a*c))/(2*a)
        x2=(-b+math.sqrt(b**2-4*a*c))/(2*a)
        return x1,x2
    else:
        return('测试失败')

print(quadratic(a,b,c))
